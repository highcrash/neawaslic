# License-server-specific ignores (in addition to the repo root .gitignore)

# Prisma client output — generated on each build, never committed.
prisma/client/
# Stale path from the original scaffold; kept here so historical clones
# stay clean if anyone ever generated to the old location.
src/generated/

# Local env
.env
.env.local

# Per-app tsconfig incremental artefacts
tsconfig.tsbuildinfo

# Nest CLI artefacts
dist/
